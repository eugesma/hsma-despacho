// Add the dialog title
$('#dialog h3').html("<i class=' glyphicon glyphicon-pencil'></i>  Editando nota de entrada");

// Render the edit form
$('.modal-body').html('<%= j render("form") %>');

// Add form button
$('.modal-footer').html(
  "<button type='button' class='btn btn-default pull-left' data-dismiss='modal'>Volver</button>"+
  "<button type='submit' name='commit' class='btn btn-primary pull-right' form='edit_entry_note_<%=@entry_note.id%>' value='Guardar cambios'>Guardar cambios</button>"
);

// Show the dynamic dialog
$('#dialog').modal("show");

// Set focus to the first element
$('#dialog').on('shown.bs.modal', function () {
  $('#entry_note_note_number').focus();
});

<%= render 'form_function' %>

$('#entry_note_entry_date').val("<%= @entry_note.entry_date.strftime('%d/%m/%Y') %>");
$('#entry_note_out_date').val("<%= @entry_note.out_date.strftime('%d/%m/%Y') %>");
